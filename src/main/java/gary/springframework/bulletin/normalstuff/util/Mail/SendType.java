package gary.springframework.bulletin.normalstuff.util.Mail;

public enum SendType {
    send,   // for registration email
    resend, // for resend registration email
    reset   // for reset password email
}
